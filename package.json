{
  "name": "biomol-explorer-workspace",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "python": "cd apps/python-service && conda run -n BioMolExplorer python app.py",
    "dev": "concurrently \"npm run dev --workspace=api\" \"npm run dev --workspace=web\" \"npm run python\"",
    "start": "concurrently \"npm run start --workspace=api\" \"npm run start --workspace=web\" \"npm run python\""
    },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}